version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          start: yarn run start
          yarn: true
          browser: chrome
          executor: cypress/base-14
